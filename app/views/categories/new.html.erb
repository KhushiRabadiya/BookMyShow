<h1>New Category</h1>
<%= form_with model: @category do |f| %>

  <div>
    <%=f.label :name %>
    <%= f.select :name, [['Sports', 'Sports'], ['Entertainment', 'Entertainment'], ['Live concerts', 'Live concerts']] %>
  </div>
  <br>
  <%= f.submit %>
<% end %>
